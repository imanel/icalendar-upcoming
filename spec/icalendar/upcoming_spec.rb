require 'spec_helper'

describe Icalendar::Upcoming do
  it 'has a version number' do
    expect(Icalendar::Upcoming::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
